/**
 * App-wide configuration.
 *
 * Chess Wrapped is a yearly recap. The target year is a single value so that
 * switching years later is a one-line change. See docs/DEVELOPMENT.md
 * (Target Year) and docs/SCENES.md (Yearly Scope).
 */

export const TARGET_YEAR = 2026;
